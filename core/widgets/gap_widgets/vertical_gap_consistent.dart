import 'package:flutter/material.dart';

class VerticalGapWidget extends SizedBox {
  /// [VerticalGapWidget] widget for creating a vertical gap with the specified [height]
  const VerticalGapWidget(double height, {super.key})
      : super(
          height: height,
        );
}
